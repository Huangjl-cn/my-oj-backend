# 代码沙箱资源统计与用例隔离改造记录

## 1. 目标与结论

本轮改造解决三个相互关联的问题：

1. 编译、容器初始化和用户程序运行混在同一个内存统计中，导致首轮数据异常偏高。
2. 同一运行容器连续执行多个用例时，cgroup 峰值、后台子进程和临时文件会跨用例残留。
3. 原来的 25 秒请求总上限会和多个合法用例冲突。

当前方案的结论如下：

- 一次请求最多创建两个容器：一个编译容器，一个可重复启动的运行容器。
- 编译容器读写挂载请求目录，完成后删除；编译资源不进入最终 `JudgeInfo`。
- 运行容器只读挂载编译产物，每个输入用例执行前启动、执行后强制停止。
- 停止并重新启动同一个运行容器会重建该容器的 cgroup 生命周期和 `/tmp` tmpfs，从而清除峰值、临时文件和全部残留进程。
- 内存优先读取 cgroup 的历史峰值，Docker stats 只作为兼容性兜底；不扣除容器基础开销。
- 用例仍按输入顺序串行执行，响应返回所有已执行用例的最大时间和最大内存。
- 单用例执行硬上限由 5 秒调整为 8 秒，仍使用现有 `Runtime Error` 状态；删除 25 秒请求总上限。
- 鉴权失败固定返回简洁的 403 JSON，同时关闭 Spring 错误响应中的异常、消息和堆栈。

这套设计没有把判题职责放进沙箱。8 秒是保护执行节点的硬上限；题目的 TLE/MLE 仍应由调用方根据 `JudgeInfo` 和题目限制判断。

## 2. 改造前的问题

调用方集成测试最初记录到以下简单程序内存：

| 语言 | Docker 启动后首轮 | 后续三次热态 |
| --- | ---: | ---: |
| Java | 118584 KB | 3.3-4.1 MB |
| C++ | 68900 KB | 3.0-3.2 MB |
| Go | 107072 KB | 39-41 MB |
| JavaScript | 99288 KB | 2.6-2.8 MB |

同一程序的首轮和热态相差数十 MB，且 Java 热态低到不符合 JVM 实际进程占用。这不是合理的语言差异，而是统计边界和采样时机不稳定造成的结果。

旧实现有三个根因：

- 编译和运行共享同一容器，容器级数据可能包含编译器、语言工具链和初始化峰值。
- Docker stats 是离散轮询。短进程可能在两次采样之间完成，采集到的值取决于调度时机。
- 多个用例共享一个持续运行的容器。即使每个用例使用独立 `docker exec`，cgroup 峰值仍会保留整个容器生命周期的最大值。

因此，单纯提高轮询频率不能从根本上修复问题。

## 3. 为什么选择 cgroup 峰值，而不是单进程 RSS/USS

### 3.1 单进程 RSS 的缺陷

RSS 能描述某个时刻一个进程驻留在物理内存中的页面，但不适合作为本沙箱的唯一口径：

- 用户程序可以创建子进程，父进程 RSS 不包含子进程。
- Java、Node.js、Python 扩展或普通本地程序都可能通过子进程完成实际工作。
- 对 `/proc/<pid>/status` 的 RSS 仍是轮询，短暂峰值可能被漏掉。
- 主进程退出后 PID 很快消失，事后无法再读取其历史峰值。

### 3.2 USS 的缺陷

USS 只统计进程独占页面。它适合诊断进程之间的共享程度，但不适合作为判题上限：共享库、运行时映射和子进程共享页都会被不同程度排除，语言之间的口径反而更难统一。USS 通常还需要遍历 `smaps`，采集成本明显高于读取一个 cgroup 文件。

### 3.3 cgroup 峰值的优势

cgroup 对容器中整棵进程树计账：

- 用户主进程和它创建的全部子进程都进入同一统计范围。
- cgroup v2 的 `memory.peak` 保留该 cgroup 生命周期内的历史峰值，短进程退出后仍可读取。
- 读取是常数级文件操作，不依赖高频采样追赶瞬时峰值。
- cgroup 同时也是 Docker 内存硬限制的执行边界，统计和限制口径一致。

当前实现依次尝试：

1. cgroup v2：`/sys/fs/cgroup/memory.peak`
2. cgroup v1：`memory.max_usage_in_bytes` 的常见挂载路径
3. Docker stats 中的 `maxUsage` 或 `usage`

最终取 cgroup 峰值和 Docker stats 已采样峰值中的较大值。这样在当前 cgroup v2 环境中使用可靠的历史峰值，在无法读取 cgroup 峰值的环境中仍保留原有兼容能力。

## 4. 是否扣除容器基础内存

最终决定是不扣除。

容器并不是一台独立虚拟机，镜像层和 Docker daemon 的内存不会全部计入容器 cgroup。当前数据中的基础开销主要来自容器 PID 1、用户进程运行时和少量 cgroup 计账；它是执行该程序的真实成本。

人为减去固定值有以下问题：

- 基线会随镜像、内核、Docker 版本和运行时变化，不是稳定常量。
- 不同语言的运行时基础内存不同，统一减值会扭曲语言差异。
- 文件页、共享页和回收时机可能使“容器开销”和“程序开销”无法严格拆分。
- 减值后可能出现负数或低估，重新引入随机 MLE/漏判 MLE。

本机实测基础峰值语言补偿和参考解大约为 5-7 MB，相对于常见的 128/256 MB 题目限制足够小。调用方应以完整进程树的原始 cgroup 峰值判题，并通过校准处理语言差异。

## 5. 两代实现演进

### 5.1 第一代：从 stats 轮询改为编译/运行分离与 cgroup 峰值

第一代改造做了两件关键工作：

1. 编译容器和运行容器分离。二者共享本次请求的唯一工作目录，但处于不同的 cgroup，编译峰值不再污染运行结果。
2. 用户程序结束后读取 `memory.peak`，并保留 Docker stats 作为兜底。

第一代解决了“首轮把编译和初始化算入运行内存”以及“短进程轮询漏采样”的问题。它仍有一个边界：如果同一个运行容器连续执行多个用例，`memory.peak` 是容器整个生命周期的峰值，不能证明每个用例独立；后台子进程和 `/tmp` 文件也可能影响后续用例。

### 5.2 第二代：同一运行容器按用例重启

第二代保留“一次请求两个容器”的性能边界，不为每个用例重新创建容器：

```text
请求工作目录
    |
    +-- 编译容器（挂载 rw）
    |      编译一次 -> 删除
    |
    +-- 运行容器（挂载 ro，只创建一次）
           用例 1：start -> exec -> 读峰值 -> kill
           用例 2：start -> exec -> 读峰值 -> kill
           ...
           请求结束 -> remove
```

具体改进：

- 运行容器只创建一次，每个用例复用其配置和容器元数据。
- 每个用例开始前 `start`，结束后在 `finally` 中 `kill`，异常、超时和正常退出都走同一清理路径。
- `kill` 停止容器 PID 1 及其 cgroup 中全部进程，后台进程不能跨用例残留。
- 再次 `start` 时 cgroup 峰值重新计数，`/tmp` tmpfs 重新挂载。
- 编译目录在运行阶段以只读方式挂载，用户程序不能修改源码、可执行文件或创建下一用例可见的工作目录文件。
- 容器 PID 1 由 shell 循环改为单个 `/bin/sleep 2147483647`，减少一个常驻 shell 及其重复创建的子进程。

同一请求内的用例仍然串行。并行执行若仍要保持独立 cgroup，只能同时启动多个独立运行容器，这会回到“每个用例一个容器”的资源成本，并改变输出顺序、并发容量和调度模型，不属于本轮改造范围。

## 6. cgroup 可行性实验

### 6.1 实验环境

- 日期：2026-08-05
- Docker Server：29.6.2
- Docker 运行环境：Docker Desktop，Linux x86_64
- 宿主：WSL2，Linux 6.18.33.2-microsoft-standard-WSL2
- cgroup driver：`cgroupfs`
- cgroup version：v2
- cgroup 挂载：容器内只读，但 `memory.peak` 可读取

### 6.2 历史峰值是否能在子进程结束后保留

Python 探针申请并写入 32 MiB 后退出，结果如下：

| 阶段 | memory.current | memory.peak |
| --- | ---: | ---: |
| 容器基础状态 | 4,591,616 B | 6,443,008 B |
| 子进程结束后 | 10,706,944 B | 48,046,080 B |

`memory.current` 在子进程结束后下降，而 `memory.peak` 仍保留约 45.8 MiB 的历史峰值。这证明无需在用户进程存活期间高频轮询，也能在退出后读取峰值。

### 6.3 轻量 PID 1 的基础开销

五个镜像分别重复 5 次，对比 shell 循环 keeper 和直接 `sleep`。下表单位为 KB：

| 语言镜像 | shell keeper P50 | shell keeper P95 | direct sleep P50 | direct sleep P95 |
| --- | ---: | ---: | ---: | ---: |
| Java | 6852 | 13668 | 6364 | 6796 |
| C++ | 6120 | 8104 | 5820 | 6128 |
| Go | 6340 | 7812 | 5624 | 5876 |
| Python | 5868 | 6348 | 5616 | 6380 |
| JavaScript | 5868 | 6340 | 6076 | 6116 |

直接使用 `sleep` 后大多数基础峰值集中在 5.5-6.8 MB，Java shell keeper 的 P95 抖动也明显消失，因此生产实现采用 direct sleep。

### 6.4 重启是否真正重置状态

对同一个 Python 运行容器连续执行 5 次 `kill -> start`，每轮申请不同大小内存，并尝试遗留后台进程和 `/tmp` 文件：

| 轮次 | 用户申请 | 运行峰值 | 下次启动基础峰值 | 临时文件隔离 | 启动后进程数 | start | kill |
| --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| 1 | 8 MiB | 14380 KB | 5816 KB | 通过 | 1 | 139 ms | 64 ms |
| 2 | 16 MiB | 25012 KB | 6120 KB | 通过 | 1 | 113 ms | 70 ms |
| 3 | 24 MiB | 30596 KB | 5828 KB | 通过 | 1 | 118 ms | 79 ms |
| 4 | 32 MiB | 40144 KB | 5572 KB | 通过 | 1 | 121 ms | 61 ms |
| 5 | 40 MiB | 47712 KB | 5872 KB | 通过 | 1 | 131 ms | 64 ms |

每次重新启动后的峰值均回到约 5.4-6.0 MB；延迟触发的后台进程没有在下一轮创建文件；启动后只存在 PID 1。由此确认同容器重启可以同时重置 cgroup 峰值、tmpfs 和进程树。

### 6.5 容器生命周期成本

同一环境下重复 5 次的平均耗时：

| 操作 | 样本 | 平均值 |
| --- | --- | ---: |
| create | 83, 71, 70, 70, 74 ms | 73.6 ms |
| start | 121, 122, 122, 114, 137 ms | 123.2 ms |
| kill | 62, 58, 58, 63, 75 ms | 63.2 ms |
| remove | 40, 36, 36, 37, 42 ms | 38.2 ms |

按用例重新创建容器约需 `create + start + kill + remove = 298 ms`；复用容器并重启约需 `start + kill = 186 ms`，每个用例平均节省约 112 ms，生命周期成本降低约 37%。这就是选择“一次请求两个容器，而不是每个用例一个容器”的直接依据。

### 6.6 只读编译产物兼容性

Java、C++、Go、Python、JavaScript 均在读写挂载中完成编译，再切换到只读挂载运行，加法程序全部正确输出 `3`。因此当前五种语言不依赖在工作目录中写入运行时文件；Python 和 Go 所需缓存已通过环境变量指向 `/tmp`。

## 7. 当前实现下的五语言实测

### 7.1 测试方法

每种语言、每种负载各发起 3 个完整沙箱请求。每次请求均重新编译并创建运行容器，但表中的时间只统计用户运行命令，内存是运行容器原始 cgroup 峰值。

镜像与编译选项和生产定义完全一致：

- C++：GCC 15，`-O2 -std=c++17`
- Java：Eclipse Temurin JDK 25
- Go：Go 1.25
- Python：Python 3.14 Alpine
- JavaScript：Node.js 24 Alpine

固定 32 MiB 分配用例会写入每个内存页并保持 300 ms，避免惰性分配。C++ 初版基准只观察 `vector.size()`，被 `-O2` 消除分配，得到约 6 MB 的无效结果；修改为读取每页并输出校验值后重新采集。文档只使用修正后的 C++ 数据。

### 7.2 简单程序

表中为 3 次的中位数，括号内为最小值到最大值：

| 语言 | 内存峰值 | 运行时间 |
| --- | ---: | ---: |
| C++ | 6088 KB (6080-6124) | 44 ms (43-64) |
| Java | 21072 KB (20100-21524) | 78 ms (76-92) |
| Go | 5576 KB (5572-6292) | 52 ms (46-61) |
| Python | 8060 KB (7220-8064) | 65 ms (56-79) |
| JavaScript | 12816 KB (12432-12824) | 87 ms (72-91) |

与最初的首轮 69-119 MB 相比，当前简单程序数据稳定在符合各语言运行时特征的范围内；Java 也不再出现不可信的 3-4 MB 热态值。

### 7.3 固定写入 32 MiB

| 语言 | 内存峰值中位数 | 范围 | 相对简单程序增加 | 时间中位数 |
| --- | ---: | ---: | ---: | ---: |
| C++ | 36832 KB | 35468-36888 KB | 30744 KB | 365 ms |
| Java | 53184 KB | 52656-53220 KB | 32112 KB | 407 ms |
| Go | 37460 KB | 37384-37968 KB | 31884 KB | 368 ms |
| Python | 38368 KB | 38116-38908 KB | 30308 KB | 384 ms |
| JavaScript | 45772 KB | 45556-45780 KB | 32956 KB | 403 ms |

五种语言相对各自简单程序的增量约为 29.6-32.2 MiB，与实际触碰的 32 MiB 一致。这比比较绝对值更能证明 cgroup 计账是线性的。绝对值中保留了容器和语言运行时开销，没有做减法。

### 7.4 固定 1000 万次整数运算

| 语言 | 内存峰值中位数 | 时间中位数 | 相对 C++ 时间 |
| --- | ---: | ---: | ---: |
| C++ | 6120 KB | 56 ms | 1.0x |
| Java | 21556 KB | 79 ms | 1.4x |
| Go | 6320 KB | 61 ms | 1.1x |
| Python | 7296 KB | 821 ms | 14.7x |
| JavaScript | 14040 KB | 89 ms | 1.6x |

这只是一个冷启动整数循环微基准，不能直接当作所有题目的语言倍率。它能说明两点：时间统计结果稳定；解释型语言在 CPU 密集题上的差异可能远大于简单 I/O 题，固定倍率必须由题目参考解复核。

## 8. 判题限制建议

### 8.1 职责划分

沙箱负责：

- 可靠执行程序并返回输出、最大运行时间和最大 cgroup 内存峰值。
- 在 8 秒和 512 MiB 的节点安全边界内终止异常程序。
- 清理进程、容器和请求工作目录。

调用方负责：

- 按题目和语言计算有效时间、内存限制。
- 比较 `JudgeInfo.time` / `JudgeInfo.memory` 并给出 TLE/MLE。
- 比较每个输出并给出 Accepted/Wrong Answer。

### 8.2 建议的初始语言补偿

以 C++ 限制为基准，可以先采用以下规则，再用各题参考解校准：

| 语言 | 时间初始倍率 | 内存初始补偿 | 依据与注意事项 |
| --- | ---: | ---: | --- |
| C++ | 1.0x | +0 MiB | 基准 |
| Java | 2.0x | +64 MiB | 本机简单程序比 C++ 多约 15 MB，32 MiB 分配场景多约 16 MB；+64 MiB 有充足 JVM/GC 余量 |
| Go | 1.5x | +32 MiB | 当前镜像数据接近 C++，补偿主要覆盖 GC、goroutine 栈和题型差异 |
| Python | 4.0x | +32 MiB | CPU 密集代码可能仍超过 4x，必须跑 Python 参考解；不应仅凭统一倍率保证所有题公平 |
| JavaScript | 2.0x | +64 MiB | Node 基础峰值比 C++ 多约 6-8 MB，额外空间覆盖 V8 heap 和 GC 波动 |

Java 使用“C++ 内存限制 + 64 MiB”的现有策略是合理且偏宽松的，无需因为容器 5-7 MB 基线再做扣减。

### 8.3 一套可直接起步的默认值

若平台需要一套通用初始配置，建议把 C++ 默认值设为 **1500 ms / 256 MiB**：

| 语言 | 时间限制 | 内存限制 |
| --- | ---: | ---: |
| C++ | 1500 ms | 256 MiB |
| Java | 3000 ms | 320 MiB |
| Go | 2250 ms | 288 MiB |
| Python | 6000 ms | 288 MiB |
| JavaScript | 3000 ms | 320 MiB |

选择 1500 ms 而不是直接给 C++ 2 秒，主要是因为固定沙箱硬上限为 8 秒：Python 4x 后为 6 秒，仍留有调整空间。若某题必须给 C++ 2 秒，可以把 Python 设为 6-7.5 秒并用参考解验证；不建议把调用方有效上限正好设为 8000 ms，否则会和沙箱硬终止边界重合。

对于特别简单的题可以从 1000 ms / 128 MiB 起步；图论、大输入 I/O 或高常数题可使用 2000-4000 ms / 256-512 MiB，但必须满足：

- 所有语言的有效时间严格小于 8 秒硬上限。
- 所有语言的有效内存不高于 512 MiB 容器硬上限。
- 如果 Java 固定加 64 MiB，C++ 题目内存最高不应超过 448 MiB；实践中推荐保留更多余量。
- 参考解应在与生产一致的镜像和 CPU 配额下至少冷启动运行 5 次，并用 P95 而不是单次最快值校准。

### 8.4 参考解校准方法

建议每道题执行以下流程：

1. 为每种允许语言准备复杂度正确、实现质量相当的参考解。
2. 覆盖最大规模和最坏路径输入，每种语言至少冷启动运行 5 次。
3. 取最大用例时间的 P95，并乘 1.5-2.0 的安全系数形成题目时间限制。
4. 取原始 cgroup 内存峰值的 P95，增加至少 16-32 MiB 或 20% 的余量。
5. 将结果限制在沙箱 8 秒/512 MiB 的硬边界内；超出时应优化题目、参考解或调整沙箱硬边界，而不是在调用方隐藏沙箱提前终止。

通用倍率适合生成初始值，参考解数据才是题目最终限制的依据。

## 9. 超时、聚合和状态语义

- 编译硬上限：10 秒。
- 每个运行用例硬上限：8 秒。
- 请求总上限：已删除。
- 用例执行方式：按 `inputList` 顺序串行。
- 返回时间：所有已执行用例中单用例运行时间最大值。
- 返回内存：所有已执行用例中单用例原始 cgroup 峰值最大值。
- 超时状态：按约定继续返回 `Runtime Error`，诊断为 `Execution timed out after 8000 ms`。

删除请求总上限后，包含 N 个合法用例的请求理论上可以运行接近 `N * 8 秒 + 编译和容器生命周期时间`。调用方 HTTP 超时和并发控制应按允许的最大用例数独立设置；不能再假设沙箱会在 25 秒内结束整个请求。

## 10. 鉴权失败响应

错误密钥不再抛出 `ResponseStatusException`，而是由控制器直接返回：

```json
{
  "status": 403,
  "message": "Forbidden"
}
```

`application.yml` 同时设置：

```yaml
server:
  error:
    include-stacktrace: never
    include-exception: false
    include-message: never
```

因此即使其他请求进入 Spring Boot 默认错误处理，也不会在响应体中暴露控制器源码行号、Spring/Tomcat 调用栈或异常类型。

## 11. 验证结果

常规测试：

```bash
mvn test -q
```

通过。

真实 Docker 集成测试：

```bash
RUN_DOCKER_TESTS=true mvn -q -Dtest=DockerSandboxIntegrationTest test
```

结果：`25` 个测试全部通过，失败 `0`，错误 `0`，耗时约 `86.1 s`。覆盖内容包括：

- 五种语言成功执行、编译错误和运行错误。
- 8 秒无限循环终止。
- 编译峰值不进入运行峰值。
- 一 MiB 输出限制、网络禁用和只读根文件系统。
- 用例间后台子进程及 `/tmp` 文件隔离。
- 缺失镜像和编译超时清理。
- 真实 HTTP 鉴权和响应序列化。

## 12. 已知边界

- cgroup 数据会随内核、Docker、镜像和语言版本变化。升级上述组件后应重跑本文基准。
- cgroup 峰值包含容器内 PID 1、用户进程树和该 cgroup 计账的内存页，但不包含宿主 Docker daemon 的全部内存。
- Docker stats 兜底仍是采样值；生产环境应确认 cgroup 峰值文件可读，并监控读取失败日志。
- 当前用例是串行执行，不能据此推断并行吞吐量。
- 512 MiB 是容器安全硬限制，不等同于所有题目的题面内存限制。
- 沙箱仍按约定把硬超时归类为 `Runtime Error`；调用方若需要展示 TLE，应根据题目时间限制和 `JudgeInfo` 转换用户可见判题结果。
